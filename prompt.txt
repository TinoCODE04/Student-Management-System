我创建了两个文件夹springboot和vue；java21; 用中文：我要开发一个学生信息管理系统。技术栈：前端采用vue3 composition api vite, javascript, router, pinia, eslint, Element-plus ; 后端采用：springboot , mybatis-plus; 数据库: mysql；本系统有2类用户，学生和教务教师 ，他们拥有不同的权限。登录（用户名+密码+验证码），学生信息管理（CRUD) 等等。 操作如下：        
                                                          
（1）前端：学生管理表格列表（含查询 + CRUD）
使用 Vue3 Composition API + Element-Plus 写一个学生信息管理页面。
功能包括：
1. 查询学生（姓名、学号、班级）
2. 显示学生表格（分页）
3. 新增学生（弹窗 Form）
4. 编辑学生（弹窗 Form）
5. 删除学生（二次确认）
6. 调用后端接口：/api/student
7. 代码结构清晰，可维护，使用 axios 封装 request。
请生成完整可运行的 Vue 代码（<template><script setup><style>）。                    

登录页面（含验证码）
使用 Vue3 Composition API + Element-Plus 编写登录页面。
要求：
1. 输入项：用户名、密码、验证码
2. 显示验证码图片（点击刷新）
3. 调用后端 /api/auth/login
4. 表单校验、loading 状态
5. 登录成功跳转到首页 /dashboard

（2）后端 Spring Boot + MyBatis-Plus
生成学生表的 CRUD 接口（Controller, Service, Mapper）
使用 Spring Boot + MyBatis-Plus 编写 Student 模块。
请生成：
1. Student 实体类（字段：id, studentId, name, gender, age, classId, phone）
2. StudentMapper
3. StudentService + ServiceImpl
4. StudentController（CRUD：分页查询、新增、更新、删除）
5. 返回类型统一使用 Result 包装类
6. 分页使用 MyBatis-Plus Page

登录 + Token 权限（JWT）系统
使用 Spring Boot 开发登录认证模块。
要求生成：
1. 用户实体类 fields: id, username, password, role
2. 使用 MyBatis-Plus 查询用户
3. 登录接口 /api/auth/login
4. 校验密码（BCrypt）
5. 生成 JWT token
6. 全局拦截器 JWT 过滤器，放行 /api/auth/**
7. 根据 role(student / teacher) 控制接口权限

（3）前后端对接 API 封装提示词
Axios 请求封装
编写 axios 封装 request.js：
1. 设置 baseURL: /api
2. 请求拦截器：自动附加 JWT token
3. 响应拦截器：处理 401 自动跳转登录
4. 错误统一弹出 ElMessage

（4）RBAC 权限控制（前端路由）
Vue 路由根据角色权限过滤
编写 Vue3 前端权限路由守卫：
1. 从 localStorage 获取 token 和 role
2. 若无 token 跳转登录页
3. 若访问 teacher 模块，必须 role = "teacher"
4. 若访问 student 模块，必须 role = "student"
5. 支持动态路由

（5）数据设计
1.我要用Mysql workbench，创建student_management_sys，用户root, 密码：010804
2.有两个角色教师和学生可以登录。
3. 功能模块
教师信息：用户名、密码、姓名、性别、职称、所属专业、角色
学生信息：用户名、密码、姓名、性别、学号、总学分、所属学院、角色
学院信息：学院名称，最低学分
专业信息：专业名称、系名、所属学院
课程信息：课程名称、学分、所属学院、上课教师、上课时段、上课地点、已选人数。
登录注册、修改密码、个人信息管理、退出登录
4. 系统角色: 
教师：教师可以看到学院信息、专业信息，但只能查看；可以查看自己的课程信息；可以查看自己课程的选课信息等等
学生：学生可以查看学院、专业信息，自己的选课表，只能查看自己的信息，成绩等等